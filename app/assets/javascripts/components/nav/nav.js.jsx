var Nav = React.createClass({

  render: function () {
    return (
      <div>
        <img className="logo" src={window.Unfarely.images.logo} />
        <div>Hey Nav</div>
      </div>
    );
  }

})
